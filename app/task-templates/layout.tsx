import { TaskAppLayout } from '../../src/components/layout/TaskAppLayout';

export default function TaskTemplatesLayout({ children }: { children: React.ReactNode }) {
  return <TaskAppLayout>{children}</TaskAppLayout>;
}
